# Add an entry, where the key represents a slug.
# Provide at least a `name` value and optionally `image`, `color` and `description`.
# Markdown is enabled for `description`, however don't use more than one paragraph (enforced by `>`)
#
# Example:
#
# ~~~
# mytag:
#   name: My Tag
#   description: >
#     *My Tag* is an example of how to define a custom tag.
# ~~~

# defaults

projects:
  name: Projects
  image:
  color:
  description: >
    Doing side projects is my favorite aspect of being in the field of computer science.
    They completely follow your own direction, pace, and creativity.

thoughts:
  name: Thoughts
  image:
  color: '#6859cc'
  description: >
    Just things I think about.
